# --- App ---
APP_TITLE=Hemograma + Bioquímica — Registro & Gráfico
SECRET_KEY=CHANGE_ME_SUPER_SECRET

# --- Database (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=teste
DB_USER=postgres
DB_PASS=84120016s

# --- OCR / PDF ---
# No Windows, aponte o executável do Tesseract:
TESSERACT_CMD=D:\\tesseract\\tesseract.exe
# No Windows, aponte a pasta /bin do Poppler (para pdf2image):
POPPLER_PATH=C:\\poppler-24.02.0\\Library\\bin
OCR_LANGS=por+eng

# Onde salvar dumps de texto extraído (opcional)
TEXT_DUMP_DIR=./pdf_text_dumps
